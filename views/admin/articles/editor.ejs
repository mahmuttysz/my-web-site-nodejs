<%- include('../partials/header') %>
    <div class="flex min-h-screen flex-col lg:flex-row">
        <%- include('../partials/sidebar') %>
            <main class="flex-1 min-w-0 p-4 sm:p-6 lg:p-8 overflow-y-auto">
                <header class="flex items-center justify-between mb-8">
                    <div>
                        <h1 class="text-2xl font-bold text-white">
                            <%= article.id ? 'Makaleyi Düzenle' : 'Yeni Makale Ekle' %>
                        </h1>
                        <p class="text-slate-400 text-sm mt-1">İçeriğinizi Markdown formatında kaleme alabilirsiniz.</p>
                    </div>
                    <div class="flex items-center gap-4">
                        <a href="<%=adminEndpoint%>/articles"
                            class="text-xs text-slate-400 hover:text-white transition-colors">
                            ← İptal ve Geri Dön</a>
                        <% if (article.id && typeof sibling !== 'undefined' && sibling) { %>
                            <a href="<%= adminEndpoint %>/articles/edit/<%= sibling.id %>"
                                class="text-xs text-sky-400 hover:text-sky-300 transition-colors">
                                <%= String(sibling.language || '').toUpperCase() %> kaydını düzenle
                            </a>
                        <% } else if (article.id && article.slug) { %>
                            <form action="<%= adminEndpoint %>/articles/translate/<%= article.id %>" method="POST" class="inline">
                                <%- include('../partials/csrf') %>
                                <button type="submit"
                                    class="text-xs text-sky-400 hover:text-sky-300 transition-colors">
                                    <%= (typeof otherArticleLang !== 'undefined' ? otherArticleLang : 'en').toUpperCase() %> taslağı oluştur
                                </button>
                            </form>
                        <% } %>
                        <% const published = Number(article.status) === 1; %>
                        <% if (article.preview_token && !published) { %>
                            <a href="<%= (article.language === 'en' ? '/en' : '') + '/blog/_preview/' + article.preview_token %>"
                                target="_blank" rel="noopener noreferrer"
                                class="text-xs text-emerald-400 hover:text-emerald-300 transition-colors">
                                Taslağı önizle ↗
                            </a>
                        <% } else if (published && article.slug) { %>
                            <a href="<%= (article.language === 'en' ? '/en' : '') + '/blog/' + article.slug %>"
                                target="_blank" rel="noopener noreferrer"
                                class="text-xs text-emerald-400 hover:text-emerald-300 transition-colors">
                                Yayındaki yazı ↗
                            </a>
                        <% } %>
                    </div>
                </header>
                <form
                    action="<%= article.id ? adminEndpoint + '/articles/edit/' + article.id : adminEndpoint + '/articles/create' %>"
                    method="POST" enctype="multipart/form-data" class="space-y-6 max-w-6xl">
                    <%- include('../partials/csrf') %>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div>
                            <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2">
                                Makale Başlığı
                            </label>
                            <input type="text" name="title" value="<%= article.title || '' %>" required
                                placeholder="Örn: Node.js ile Mükemmel Performans"
                                class="w-full bg-slate-900 border border-slate-800 rounded-lg px-4 py-3 text-white text-sm focus:outline-none focus:border-blue-500" />
                        </div>
                        <div>
                            <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2">
                                URL Slug (İsteğe Bağlı)
                            </label>
                            <input type="text" name="slug" value="<%= article.slug || '' %>"
                                placeholder="nodejs-ile-mukemmel-performans"
                                class="w-full bg-slate-900 border border-slate-800 rounded-lg px-4 py-3 text-slate-300 text-sm focus:outline-none focus:border-blue-500">
                        </div>
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2">
                            Kısa Özet (Excerpt)
                        </label>
                        <textarea name="excerpt" rows="2"
                            placeholder="Listeleme sayfalarında görünecek kısa açıklama..."
                            class="w-full bg-slate-900 border border-slate-800 rounded-lg px-4 py-3 text-white text-sm focus:outline-none focus:border-blue-500"><%= article.excerpt || '' %></textarea>
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2">
                            Kapak Görseli
                        </label>
                        <% if (article.cover_image) { %>
                            <div class="mb-3 flex items-center gap-4">
                                <img src="<%= article.cover_image %>"
                                    class="w-20 h-12 object-cover rounded border border-slate-800" />
                                <input type="hidden" name="existing_cover_image" value="<%=article.cover_image%>" />
                                <span class="text-xs text-slate-500">
                                    Mevcut Kapak Görseli
                                </span>
                            </div>
                            <% } %>
                                <input type="file" name="cover_image" accept="image/*"
                                    class="block w-full text-sm text-slate-400 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-xs file:font-semibold file:bg-slate-800 file:text-slate-300 hover:file:bg-slate-700">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2">
                            İçerik (Markdown Formatında)
                        </label>
                        <div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
                            <textarea name="content" id="article-content" rows="18" required
                                data-preview-url="<%= adminEndpoint %>/articles/preview-markdown"
                                placeholder="# Başlık 1&#10;&#10;Paragraf metni buraya gelecek..."
                                class="w-full bg-slate-900 border border-slate-800 rounded-lg p-4 font-mono text-sm text-slate-200 focus:outline-none focus:border-blue-500 leading-relaxed min-h-[28rem]"><%= article.content || '' %></textarea>
                            <div class="bg-slate-950 border border-slate-800 rounded-lg overflow-hidden min-h-[28rem] flex flex-col">
                                <p class="text-[11px] font-semibold uppercase tracking-wider text-slate-500 px-4 py-2 border-b border-slate-800">
                                    Canlı önizleme
                                </p>
                                <div id="article-md-preview"
                                    class="flex-1 overflow-y-auto p-4 text-slate-300 text-sm leading-relaxed space-y-4 [&_h1]:text-2xl [&_h1]:font-bold [&_h1]:text-white [&_h2]:text-xl [&_h2]:font-bold [&_h2]:text-white [&_h3]:text-lg [&_h3]:font-semibold [&_h3]:text-white [&_p]:text-slate-300 [&_a]:text-sky-400 [&_a]:underline [&_ul]:list-disc [&_ul]:pl-5 [&_ol]:list-decimal [&_ol]:pl-5 [&_code]:bg-slate-900 [&_code]:px-1.5 [&_code]:py-0.5 [&_code]:rounded [&_pre]:bg-slate-900 [&_pre]:p-3 [&_pre]:rounded-lg [&_blockquote]:border-l-4 [&_blockquote]:border-sky-500 [&_blockquote]:pl-4 [&_blockquote]:italic [&_img]:max-h-48 [&_img]:rounded">
                                    <p class="text-slate-600">Önizleme burada görünür.</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="flex items-center justify-between pt-4 border-t border-slate-800">
                        <div class="flex items-center gap-4">
                            <label class="text-xs font-semibold text-slate-400 uppercase tracking-wider">
                                Yayın Durumu:
                            </label>
                            <select name="status"
                                class="bg-slate-900 border border-slate-800 text-sm text-white rounded-lg px-3 py-2 focus:outline-none">
                                <option value="0" <%=article.status===0 ? 'selected' : '' %>>
                                    Taslak
                                </option>
                                <option value="1" <%=article.status===1 ? 'selected' : '' %>>
                                    Yayınla
                                </option>
                            </select>
                        </div>
                        <div class="flex flex-col gap-1">
                            <div class="flex items-center gap-4">
                                <label class="text-xs font-semibold text-slate-400 uppercase tracking-wider">
                                    Dil:
                                </label>
                                <select name="language"
                                    class="bg-slate-900 border border-slate-800 text-sm text-white rounded-lg px-3 py-2 focus:outline-none">
                                    <option value="tr" <%=article.language==='tr' ? 'selected' : '' %>>
                                        Türkçe
                                    </option>
                                    <option value="en" <%=article.language==='en' ? 'selected' : '' %>>
                                        İngilizce
                                    </option>
                                </select>
                            </div>
                            <p class="text-[11px] text-slate-500">Çeviri kaydı aynı slug ile eşleşir.</p>
                        </div>
                        <button type="submit"
                            class="px-6 py-2.5 bg-blue-600 hover:bg-blue-500 text-white rounded-lg text-sm font-medium transition-colors">
                            <%= article.id ? 'Değişiklikleri Kaydet' : 'Makaleyi Kaydet' %>
                        </button>
                    </div>
                </form>
            </main>
    </div>
    <script src="/js/article-editor.js" defer></script>
    <%- include('../partials/footer') %>